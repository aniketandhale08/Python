# deciaml is particularly useful when you need high precision in calculations, such as in financial or scientific applications.

"""
import decimal

num = int(input("Enter the num: "))

print(decimal.Decimal(num))
print(type(decimal.Decimal(num)))

 """



# string of int to the decimal

import decimal
str = "1234"

print(decimal.Decimal(str))
print(type(decimal.Decimal(str)))
